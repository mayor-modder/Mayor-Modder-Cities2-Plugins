# {{DISPLAY_NAME}} (Hybrid)

{{SHORT_DESCRIPTION}}

## Build
```bash
dotnet build -c Release
```

The C# project triggers the UI build step from `ui/`.

## Compatibility
- Cities: Skylines II `{{GAME_VERSION}}`
